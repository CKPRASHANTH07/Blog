{
  "name": "server",
  "version": "1.0.0",
  "description": "blog app's backend",
  "main": "server.js",
  "scripts": {
    "server": "nodemon server.js",
     "build": "npm install && npm install --prefix Frontend && npm run build --prefix Frontend"
  },
  "keywords": [],
  "author": "Ckprashanth",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "mongosh": "^2.2.2"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.1.0"
  }
}
